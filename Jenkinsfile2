#!/usr/bin/env groovy

@Library('sharedlibrary-demo2@master') _ //master or whatever branch

pipeline{

      agent {
                docker {
                image 'maven:3-openjdk-11'
                }
            }
        
        stages{

              stage('maven build'){
                  steps{
                      script{
		    	                sh "mvn clean install"
                      	  }
               	     }  
                 }	
                 
                 stage ('Check logs') {
                    steps {
                        filterLogs ('WARNING', 5)
                    }
                }
		
           }	       	     	         
}
